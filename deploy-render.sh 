#!/bin/bash
echo "🚀 Деплой на Render..."

if [ -z "$1" ]; then
    echo "Использование: ./deploy-render.sh YOUR_BOT_TOKEN"
    exit 1
fi

echo "1. Создайте аккаунт на https://render.com"
echo "2. Подключите GitHub репозиторий"
echo "3. Создайте новый Web Service"
echo "4. Укажите настройки:"
echo "   - Build Command: pip install -r backend/requirements.txt"
echo "   - Start Command: uvicorn backend.app.main:app --host 0.0.0.0 --port \$PORT"
echo "   - Environment Variables:"
echo "     * TELEGRAM_BOT_TOKEN=$1"
echo "     * SECRET_KEY=$(openssl rand -hex 32)"
echo "     * ENVIRONMENT=production"
echo "5. Добавьте PostgreSQL базу данных из Add-ons"

echo "✅ Следуйте инструкциям в Render Dashboard"
